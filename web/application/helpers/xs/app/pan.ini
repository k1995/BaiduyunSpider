project.name=pan
[fid]
type=id

[title]
type=title

[uk]
index = self
tokenizer = full

[shorturl]

[isdir]
index = self
tokenizer = full

[size]

[md5]
index = self
tokenizer = full

[shareid]

[d_cnt]
type = numeric

[ext]
index = self
tokenizer = full

[create_time]
type = numeric

[feed_time]
type = numeric

[file_type]
index = self
tokenizer = full

[feed_type]
index = self
tokenizer = full